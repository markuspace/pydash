agent Helper {
    name: "Helper"
    role: "assist with file operations"
}

tool read_file(path: str) -> str {
    with open(path) as f:
        return f.read()
}

content = read_file("README.md")
print(content[:200])
